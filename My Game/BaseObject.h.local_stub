#pragma once
#include "Defines.h"
#include <string>

// Minimal base object used by CObject
class LBaseObject {
public:
    bool m_bDead = false;
    Vector2 m_vPos = Vector2::Zero;
    XMFLOAT4 m_f4Tint = XMFLOAT4(1.0f, 1.0f, 1.0f, 1.0f);
    virtual ~LBaseObject() {}
};
